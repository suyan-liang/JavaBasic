就是选择哪个容器

指定css要选择的容器

========================================================================================================================

类选择器
每个标签都有class属性，代表所属的小组别，谁和谁是一组，选择组进行样式化

类选择器优先级高于标签

越少数，优先级越高



可以对不同标签进行相同样式化


可以搞预定义样式，可以实现动态样式化
========================================================================================================================
优先级

标签选择器(style标签) < 类选择器 < id选择器 < style属性